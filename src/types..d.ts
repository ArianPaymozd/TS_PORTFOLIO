type Point = {
  x: number,
  y: number
}

type CarouselItem = {
  img: string,
  title: string,
  caption?: string
}

type CarouselScroll = {
  opacity: number, marginTop: number, transform?: string
}